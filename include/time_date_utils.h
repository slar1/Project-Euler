#ifndef TIME_DATE_UTILS_H
#define TIME_DATE_UTILS_H

/*
 * @file time_date_utils.h
 * @brief Functions for times and dates
 */

/*
 * @brief Determine if year is a leap year
 * @param Integer value y
 * @return Integer. 1 if leap year, 0 if not leap year
 */
int is_leap_year(int y);

#endif
